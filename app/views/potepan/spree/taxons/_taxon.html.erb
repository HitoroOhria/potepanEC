<li>
  <%= link_to potepan_category_path(taxon.id) do %>
    <i class="fa fa-caret-right" aria-hidden="true"></i><%= taxon.name %>
    <span>
      (<%= taxon.self_and_descendants.inject(0) { |product_counter, taxon| product_counter + taxon.products.count } %>)
    </span>
  <% end %>
</li>